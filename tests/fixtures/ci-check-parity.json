{
  "_comment": "Justified divergences between the `just check` target list and .github/workflows/ci.yml job names. Every entry MUST carry a non-empty reason; false records a known, deliberate omission.",
  "divergences": [
    {
      "name": "classify-changes",
      "present_in_just_check": false,
      "reason": "Hosted changed-files classifier for pull-request and canonical-push base/head pairs. Local just check has no event pair to classify; its production script is exercised by check-docs-only-ci instead."
    },
    {
      "name": "check-charters",
      "present_in_ci": false,
      "reason": "Runs in the janitor/local aggregate to enforce shipped charter-defect detectors over this repo's two enumerated charters; this factory slice may not edit .github/workflows/ to add the matching CI matrix job."
    },
    {
      "name": "check-ci-parity",
      "present_in_ci": false,
      "reason": "This is the janitor/local aggregate guard introduced by livespec-console-beads-fabro-ccycuk. It reads CI's committed workflow but this factory slice may not edit .github/workflows/ to add a new CI job."
    },
    {
      "name": "check-fork-drift",
      "present_in_ci": false,
      "reason": "Runs in the janitor/local aggregate because its upstream lane depends on installed orchestrator plugin state that CI does not provision; the gate itself prints a loud skip when that lane cannot resolve."
    },
    {
      "name": "check-fuzz",
      "present_in_just_check": false,
      "reason": "Merge-gate fuzz run (livespec-console-beads-fabro-txtzn5.9). Deliberately absent from the `just check` aggregate: SPECIFICATION/non-functional-requirements.md ratifies that `just check` MUST NOT include fuzz runs. At >=60s per target across three targets it is also a minutes-long floor by construction, which is not what a local pre-commit aggregate is for."
    },
    {
      "name": "check-mutants",
      "present_in_just_check": false,
      "reason": "Merge-gate mutation run (livespec-console-beads-fabro-txtzn5.10). Deliberately absent from the `just check` aggregate: SPECIFICATION/non-functional-requirements.md ratifies that `just check` MUST NOT include mutation runs. It also cannot run there \u2014 it needs a base ref to diff against, which a local aggregate invocation has no way to supply."
    },
    {
      "name": "check-no-workflow-edits",
      "present_in_ci": false,
      "reason": "Runs in the janitor/local aggregate only, BY DESIGN (livespec-dev-tooling-fy02): the pack body dev-tooling/check-no-workflow-edits.sh is an authorship control at the agent boundary and is a deliberate no-op when GITHUB_ACTIONS is set \u2014 CI has no local author to control, and the fleet's bot lanes (pin bumps, the canonical-slug reconciler, release-please) legitimately rewrite workflow files there. Master is protected by PR gate \u2261 master gate, which this guard neither weakens nor strengthens, so a CI job would be vacuous rather than merely un-added."
    },
    {
      "name": "check-plan-anchor-declared",
      "present_in_ci": false,
      "reason": "Runs in the janitor/local aggregate to enforce the opt-in plan lifecycle anchor convention; this factory slice may not edit .github/workflows/ to add a new CI job."
    },
    {
      "name": "check-red-green-replay",
      "present_in_ci": false,
      "reason": "Runs in the janitor/local aggregate to validate origin/master..HEAD Rust commits before Stage 3 commit-msg enforcement; this factory slice may not edit .github/workflows/ to add a new CI job."
    },
    {
      "name": "check-spec-fenced-block-staleness",
      "present_in_ci": false,
      "reason": "Fenced-block staleness gate (livespec-console-beads-fabro-4jb3kl.6), the same shape as check-red-green-replay above: it diffs the working tree against the merge base with origin/master, and the matrix job's shared actions/checkout runs at the default fetch-depth 1, where neither that ref nor any history exists — the gate would resolve no base and skip, so a CI job would be vacuous until the checkout fetches history for every target. It runs where the diff is real: the lefthook pre-push aggregate and the post-merge janitor. This factory slice also may not edit .github/workflows/."
    },
    {
      "name": "check-spec-governance-default-block",
      "present_in_ci": false,
      "reason": "Runs in the janitor/local aggregate as a hermetic spec-governance guard; CI's matrix predates this target and this factory slice may not edit .github/workflows/ to add a new CI job."
    },
    {
      "name": "ci-green",
      "present_in_just_check": false,
      "reason": "Aggregate branch-protection rollup job, not an executable check target."
    },
    {
      "name": "export-telemetry",
      "present_in_just_check": false,
      "reason": "Closed-loop Honeycomb CI telemetry export (the fleet-standard export-ci-telemetry.sh); needs gh API access to the run's own job data and a Honeycomb ingest secret, neither of which a local `just check` invocation has."
    },
    {
      "name": "check-plan-record-conformance",
      "present_in_ci": false,
      "reason": "Plan-record conformance (dev-tooling >= 1.52) reads this tenant's beads ledger, which CI cannot reach (no tenant secret in CI), and it self-skips unless LIVESPEC_RUN_PLAN_RECORD_CONFORMANCE is set; it runs armed by hand under the credential wrapper and, once the tenant is clean, in the host-side aggregate (orchestrator plan arm-plan-record-conformance, console item livespec-console-beads-fabro-pzbdbo.16)."
    }
  ]
}
