work_item=livespec-console-beads-fabro-ydlant
reason=Seed the runner pool's warmed ASAN fuzz target generation into check-fuzz's compile phase (move the provisioner's reflink seed to fuzz/target and restore source mtimes for files unchanged vs the generation's commit); fail-soft, always exit 0, so the hosted lane and a missing seed still build cold exactly as before. Maintainer-approved 2026-09-06 under plan optimize-console-builds (approval:workflow-edit on ydlant).
