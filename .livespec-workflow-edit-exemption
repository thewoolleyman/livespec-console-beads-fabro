work_item=livespec-console-beads-fabro-pzbdbo.11
reason=Base-branch-side required check that refuses a PR whose head lacks the upstream-dependency gate wiring; closes the old-worktree bypass found in the 2026-09-06 verification pass under the retire-overseer plan. Reads one file via the contents API; never executes head code.
