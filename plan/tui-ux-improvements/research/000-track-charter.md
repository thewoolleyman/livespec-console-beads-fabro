# TUI UX improvements — track charter

Parallel track of `plan/retire-overseer-and-redesign-control-plane-around-console/` (maintainer ruling 2026-09-08), anchored on epic `livespec-console-beads-fabro-mx9u` and one of that plan's exit criteria. Every dogfood pass at the real TUI writes the graded report defined in `../retire-overseer-and-redesign-control-plane-around-console/research/tui-ux-rubric.md` on the work item it exercised; when that item closes, a Fable-model agent turns the report into improvement items under this epic (label `track:tui-ux`, gradeable acceptance criteria in the field, deduped against open children), which are dispatched as filed and reported on at least every two hours. This directory exists because the plan-record contract holds that every epic is a plan and carries the slug its directory is keyed on; the reasoning, the rubric and the rulings stay in the parent plan's research notes, and status lives in the ledger, not here.
